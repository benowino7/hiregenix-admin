services:
  hiregenix_admin:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: hiregenix_admin
    environment:
      - VIRTUAL_HOST=dashboard.hiregenix.ai
      - VIRTUAL_PORT=80
      - LETSENCRYPT_HOST=dashboard.hiregenix.ai
      - LETSENCRYPT_EMAIL=isaac.ochieng2000@gmail.com
    expose:
      - "80"
    networks:
      - proxy
    restart: unless-stopped
networks:
  proxy:
    external: true
